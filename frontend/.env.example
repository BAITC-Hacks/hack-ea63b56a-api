# Server-only origin for the NestJS API. Docker Compose: http://backend:3001
BACKEND_URL=http://localhost:3001

# Browser API base path. The default uses the Next.js proxy above.
# For direct browser requests, include /api/v1 and enable CORS on NestJS.
NEXT_PUBLIC_API_URL=/api/v1
